<div>
    {{#each posts}}
    <div>
        <div>
            <h2>
                <a href="/posts/{{id}}">{{title}}</a>
            </h2>
            <div>
                 Posted by {{User.name}} 
                {{!-- {{format_date createdAt}}   --}}
            </div>
        </div>
        <div>
            {{body}}
        </div>
    </div>
    {{/each}}
</div>